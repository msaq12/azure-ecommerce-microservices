apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: furniture-dev
  labels:
    app: order-service
spec:
  type: LoadBalancer
  selector:
    app: order-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  sessionAffinity: None
